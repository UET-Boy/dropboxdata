ub fn p1q2_fun() {
    let chocolate1 = 10;
    let chocolate2 = 10;
    let total: u32 = chocolate1 + chocolate2; // Const. can't be used because both are variables
    println!("Sum of chocolate1 and chocolate2 is: {}",total);
}